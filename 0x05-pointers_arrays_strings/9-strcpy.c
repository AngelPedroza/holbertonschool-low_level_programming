#include "holberton.h"
#include <stdio.h>

/**
 * *_strcpy - check the code for Holberton School students.
 * @dest:
 * @src:
 * Return: Return.
 */
char *_strcpy(char *dest, char *src)
{
	int a;

	for (a = 0; a[src] != '\0'; a++)
	{
		a[dest] = a[src];
	}
	dest = src;
	return (dest);
}
