#include "holberton.h"
#include <stdio.h>

/**
 * mul - multiplies 2 numbers.
 * @a: The first number for multiplie
 * @b: The second number to multiplie
 * Return: the final valeu befor to multiplie.
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
